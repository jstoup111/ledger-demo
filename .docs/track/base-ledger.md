# Track: base-ledger

Track: product

The base ledger's surface is user-facing — a projector-legible page showing an account's derived
balance and transaction log, a form a presenter posts transactions from on stage, and validation
failures the audience is meant to read from across the room. Those are product requirements worth
enumerating as FRs, so `/prd` runs.
