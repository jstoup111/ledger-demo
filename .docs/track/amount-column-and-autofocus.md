# Track: amount-column-and-autofocus

Track: product

Both changes alter what the audience sees and how the presenter interacts with the page: the
transaction amounts become scannable at projector distance, and the presenter can type an amount
without first clicking. On this project legibility and presenter ergonomics are functional
requirements rather than preferences, so they are worth enumerating as FRs and `/prd` runs.
